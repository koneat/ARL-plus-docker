# Enhanced Worker image context

This directory is consumed by `enhanced-worker/Dockerfile` with the repository root as Docker build context. Production credentials, runtime reports, screenshots, PoCs and scanner caches are excluded through the root `.dockerignore`.

Do not place API keys, VLESS links, MongoDB URIs or generated reports in this directory.
